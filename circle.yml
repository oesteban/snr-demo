machine:
  python:
    version: 2.7

dependencies:
  pre:
    - wget https://bootstrap.pypa.io/get-pip.py && python get-pip.py
    - pip install numpy
    - pip install scipy
    - pip install nose

test:
  override:
    - nosetests --with-doctest .